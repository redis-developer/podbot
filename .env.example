# Example environment configuration for Agents & Arbiters
# Copy this file to .env and fill in your actual values

# OpenAI API Key
# (required, for Agent Memory Server and chat-api)
OPENAI_API_KEY=<your_open_ai_key>

# Authentication mode
# (optional, for Agent Memory Server, overrides values in docker-compose.yml)
AUTH_MODE=disabled

# Log level
# (optional, for Agent Memory Server, overrides values in docker-compose.yml)
# LOG_LEVEL=DEBUG

# Maximum context window size for the Agent Memory Server
# (optional, for Agent Memory Server but passed in to AMS by chat-api, overrides values in docker-compose.yml)
# AMS_CONTEXT_WINDOW_MAX=500
